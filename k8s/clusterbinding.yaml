# =============================================================================
# DEPRECATED — This file has been replaced by rbac.yaml
# =============================================================================
# The old cluster-admin binding has been removed for security.
# All RBAC resources are now defined in k8s/rbac.yaml with least-privilege
# permissions aligned to the guardrail (app/agents/guardrail.py).
#
# To apply the correct RBAC:
#   kubectl apply -f k8s/rbac.yaml
# =============================================================================